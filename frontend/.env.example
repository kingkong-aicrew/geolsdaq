# Frontend env — 백엔드 API URL
# NEXT_PUBLIC_* 는 빌드 시 번들에 인라인됨 (feedback_frontend_env_not_safe.md)
# 비밀 X — 공개 URL 만
NEXT_PUBLIC_API_BASE=http://localhost:8000
# canonical/OG URL 의 origin. 배포 시 https 도메인으로 교체
NEXT_PUBLIC_SITE_ORIGIN=http://localhost:3000
