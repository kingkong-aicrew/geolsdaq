fastapi==0.115.5
uvicorn[standard]==0.32.0
pydantic==2.9.2
httpx==0.27.2
slowapi==0.1.9
python-dotenv==1.0.1

# 배치 (cron 환경에서만 필요)
# KR 종목 EOD = pykrx (개별종목 기간조회만 사용 — KRX 직통, 안티봇 무관).
# US 주식·환율 EOD = Twelve Data REST(httpx) — yfinance 안티봇 차단으로 2026-06 교체.
# ⚠️ setuptools<81 고정: pykrx 1.0.45 가 pkg_resources(import) 에 의존하는데
#    setuptools 81+ 는 pkg_resources 를 제거함 → ModuleNotFoundError. 최신 pip 가
#    setuptools 82 를 깔면 cron(GitHub Actions)·로컬 모두 pykrx import 가 깨지므로 핀.
setuptools<81
pykrx==1.0.45
pandas==2.2.3

# 테스트
pytest==8.3.3
pytest-asyncio==0.24.0
